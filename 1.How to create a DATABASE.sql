-- How to create a database
create database brodb;

-- to use particular db
use brodb;

-- setting db to read only
alter database brodb READ only = 1;

-- disabling read only mode
alter database brodb read only = 0;